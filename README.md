The benchmarking was done over a short training window, which may have affected the results. Particularly, the task8 improved algorithm underperformed and was outcompeted by the task6 in terms of episode return, avg20 return, and error rate. Task7 algorithm also performed noticibly poorly but this is because it was only run for a single proccess, so this is expected. Overall this script is a usefull proof of concept and a good way to test/benchmark all the algorithms at once
